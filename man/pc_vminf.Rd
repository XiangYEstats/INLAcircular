% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{pc_vminf}
\alias{pc_vminf}
\alias{pc.vminf}
\alias{dpc.vminf}
\alias{ppc.vminf}
\alias{qpc.vminf}
\alias{rpc.vminf}
\title{PC Prior for von Mises Concentration: Point-Mass Base}
\usage{
dpc.vminf(kappa, lambda, log = FALSE)

ppc.vminf(q, lambda, log.p = FALSE)

qpc.vminf(p, lambda, log.p = FALSE)

rpc.vminf(n, lambda)
}
\arguments{
\item{kappa, q}{Vector of non-negative concentration values.}

\item{lambda}{Positive rate parameter.}

\item{log}{Logical; if \code{TRUE}, return the log-density.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are supplied or
returned on the log scale.}

\item{p}{Vector of probabilities.}

\item{n}{Number of observations. If \code{length(n) > 1}, its length is
used.}
}
\value{
\code{dpc.vminf} gives the continuous density, \code{ppc.vminf} gives the
distribution function, \code{qpc.vminf} gives the quantile function, and
\code{rpc.vminf} generates random deviates.
}
\description{
Density, distribution function, quantile function, and random generation for
the penalized-complexity prior on the von Mises concentration parameter with
the point-mass limit (\eqn{\kappa\to\infty}) as its base model.
}
\details{
The PC distance from the point-mass base model is
\deqn{d_\infty(\kappa)=\sqrt{1-I_1(\kappa)/I_0(\kappa)}.}
The continuous density and CDF are
\deqn{\pi_\infty(\kappa)=
  \lambda\exp\{-\lambda d_\infty(\kappa)\}|d_\infty'(\kappa)|,}
\deqn{F_\infty(\kappa)=\exp\{-\lambda d_\infty(\kappa)\}.}

Because \eqn{d_\infty(0)=1}, the CDF contains boundary mass
\eqn{\exp(-\lambda)} at \eqn{\kappa=0}. \code{dpc.vminf} returns the
continuous density component; \code{ppc.vminf}, \code{qpc.vminf}, and
\code{rpc.vminf} include the boundary mass. All numerical calculations,
including the large-\eqn{\kappa} asymptotic branch and quantile inversion,
are performed by compiled C code.
}
\examples{
dpc.vminf(kappa = c(0, 1, 5), lambda = 2)
ppc.vminf(q = 5, lambda = 2)
qpc.vminf(p = 0.5, lambda = 2)
rpc.vminf(n = 10, lambda = 2)
}
